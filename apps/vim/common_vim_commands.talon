tag: user.common_vim_commands
-
# vim helpers
clipboard copy: 
    key('" + y')
clipboard paste: 
    key('" + g P')
clipboard delete:
    key('" + d')
vim save:
    key(': w enter')
vim exit:
    key(': q enter')
vim exit hard:
    key(': q ! enter')
vim save exit:
    key(': w q enter')
